{
  "name": "athlete-apps",
  "private": true,
  "scripts": {
    "dev:basketball": "turbo run dev --filter=basketball",
    "dev:gym": "turbo run dev --filter=gym",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "turbo": "^2.1.3",
    "typescript": "^5.6.3"
  },
  "packageManager": "pnpm@12.4.2",
  "engines": {
    "node": ">=20"
  }
}
